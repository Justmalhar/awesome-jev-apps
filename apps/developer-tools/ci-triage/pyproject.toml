[project]
name = "ci-triage"
version = "0.1.0"
description = "Classify CI failures: real bug, flake, infra, dependency or config."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
