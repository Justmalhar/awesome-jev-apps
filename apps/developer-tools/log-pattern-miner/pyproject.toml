[project]
name = "log-pattern-miner"
version = "0.1.0"
description = "Cluster log lines into named failure modes. Regex maps, Jev judges, Python reduces."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
