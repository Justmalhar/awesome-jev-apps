[project]
name = "changelog-classifier"
version = "0.1.0"
description = "Sort merged PRs into release-note sections. Selection, never generation."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
