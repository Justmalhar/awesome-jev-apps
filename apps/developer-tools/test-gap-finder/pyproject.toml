[project]
name = "test-gap-finder"
version = "0.1.0"
description = "Which behaviours have no test? ast lists them, difflib blocks, Jev matches."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
