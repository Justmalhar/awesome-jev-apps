[project]
name = "pr-risk-scorer"
version = "0.1.0"
description = "Score a git diff for blast radius; reweight the policy without re-running inference."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
