[project]
name = "budget-variance"
version = "0.1.0"
description = "Variance computed in pandas; the stated cause classified by Jev."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
