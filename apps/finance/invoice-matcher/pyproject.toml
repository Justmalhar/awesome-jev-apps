[project]
name = "invoice-matcher"
version = "0.1.0"
description = "Three-way match of purchase order, invoice and delivery note."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
