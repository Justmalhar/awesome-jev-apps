[project]
name = "tax-classifier"
version = "0.1.0"
description = "Triage expenses into plausibly-deductible piles. Not tax advice."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
