[project]
name = "receipt-triage"
version = "0.1.0"
description = "Match receipts to card transactions and flag the unmatched ones."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
