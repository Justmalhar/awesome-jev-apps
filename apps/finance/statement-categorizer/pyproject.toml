[project]
name = "statement-categorizer"
version = "0.1.0"
description = "Categorize bank/card transactions. Judgment by Jev, arithmetic by pandas."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
