[project]
name = "expense-policy-checker"
version = "0.1.0"
description = "Check expense claims against a written policy with veto semantics."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
