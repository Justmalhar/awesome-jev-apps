[project]
name = "fraud-signals"
version = "0.1.0"
description = "Turn payment narratives into risk features for a downstream model."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
