[project]
name = "earnings-tagger"
version = "0.1.0"
description = "Tag earnings-call passages for guidance changes, hedging and tone shifts."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
