[project]
name = "calendar-triage"
version = "0.1.0"
description = "Which meetings you could skip, shorten, or delegate, judged against your stated priorities."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
