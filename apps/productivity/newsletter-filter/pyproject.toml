[project]
name = "newsletter-filter"
version = "0.1.0"
description = "Keep only the items in a newsletter digest you would genuinely have read."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
