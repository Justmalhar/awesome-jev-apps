[project]
name = "task-prioritizer"
version = "0.1.0"
description = "Re-rank a task list against this week's goal, reusing goal-independent judgments."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
