[project]
name = "live-moderation"
version = "0.1.0"
description = "Moderate a live chat stream message by message, inside the send path."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
