[project]
name = "form-validator"
version = "0.1.0"
description = "Semantic field validation while the user types, in one request per pass."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
