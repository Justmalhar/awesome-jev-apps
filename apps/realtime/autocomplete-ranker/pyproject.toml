[project]
name = "autocomplete-ranker"
version = "0.1.0"
description = "Rank the whole autocomplete candidate set by intent, in one request."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
