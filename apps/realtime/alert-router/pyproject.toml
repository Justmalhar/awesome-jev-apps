[project]
name = "alert-router"
version = "0.1.0"
description = "Page, ticket, or ignore — decided inside the alerting path, against a latency budget."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
