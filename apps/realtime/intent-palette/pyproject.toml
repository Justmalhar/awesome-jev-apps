[project]
name = "intent-palette"
version = "0.1.0"
description = "A command palette that ranks the whole action catalog on every keystroke."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
