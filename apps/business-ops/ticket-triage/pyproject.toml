[project]
name = "ticket-triage"
version = "0.1.0"
description = "Route support tickets by issue type, product area, urgency and churn risk in one request."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
