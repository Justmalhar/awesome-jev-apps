[project]
name = "churn-signals"
version = "0.1.0"
description = "Extract early churn-warning features from support and sales conversations. Features, not a verdict."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
