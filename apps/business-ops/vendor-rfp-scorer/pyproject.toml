[project]
name = "vendor-rfp-scorer"
version = "0.1.0"
description = "Score supplier RFP responses against weighted criteria with mandatory gates. Weights are free to change."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
