[project]
name = "onboarding-gaps"
version = "0.1.0"
description = "Find the onboarding steps a new hire's documentation never actually explains."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
