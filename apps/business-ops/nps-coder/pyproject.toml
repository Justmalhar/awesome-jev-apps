[project]
name = "nps-coder"
version = "0.1.0"
description = "Theme open-ended NPS and survey comments against a codebook. One Noul per theme."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
