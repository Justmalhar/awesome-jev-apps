[project]
name = "renewal-risk"
version = "0.1.0"
description = "Composite renewal risk built in code from raw, reusable account judgments."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
