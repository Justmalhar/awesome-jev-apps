[project]
name = "method-gap-finder"
version = "0.1.0"
description = "Reporting-checklist gaps in a methods section as independent veto conditions."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
