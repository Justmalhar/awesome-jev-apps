[project]
name = "dataset-license"
version = "0.1.0"
description = "Triage a stated research intent against dataset licence text. Not legal advice."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
