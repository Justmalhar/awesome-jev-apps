[project]
name = "data-extractor"
version = "0.1.0"
description = "Verbatim PICO extraction from methods sections by selection, never generation."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
