[project]
name = "peer-review-router"
version = "0.1.0"
description = "Match conference submissions to reviewers by expertise, with conflicts as a veto."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
