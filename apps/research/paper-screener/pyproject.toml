[project]
name = "paper-screener"
version = "0.1.0"
description = "Title/abstract screening for systematic reviews against a real protocol."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
