[project]
name = "entity-resolver"
version = "0.1.0"
description = "Find the duplicate records two merged systems left behind. Blocking in Python, judgment by Jev."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
