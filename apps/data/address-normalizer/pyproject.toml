[project]
name = "address-normalizer"
version = "0.1.0"
description = "Reconcile inconsistent address strings into structured fields. Python enumerates the readings, Jev ranks them."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
