[project]
name = "label-auditor"
version = "0.1.0"
description = "Find mislabelled and ambiguous rows in an existing labelled dataset."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
