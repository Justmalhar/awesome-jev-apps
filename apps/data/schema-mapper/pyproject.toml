[project]
name = "schema-mapper"
version = "0.1.0"
description = "Map a legacy export's columns onto a new schema's fields, sample values included."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.2"]
