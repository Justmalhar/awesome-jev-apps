[project]
name = "rag-reranker"
version = "0.1.0"
description = "Rerank retrieved passages, with a pure-stdlib cosine baseline to compare against."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
