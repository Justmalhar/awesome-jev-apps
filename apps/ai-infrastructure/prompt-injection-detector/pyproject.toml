[project]
name = "prompt-injection-detector"
version = "0.1.0"
description = "Catch instructions hidden inside retrieved or user-supplied content."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
