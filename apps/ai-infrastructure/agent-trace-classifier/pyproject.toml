[project]
name = "agent-trace-classifier"
version = "0.1.0"
description = "Map-reduce classification of why agent runs failed, across many traces."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
