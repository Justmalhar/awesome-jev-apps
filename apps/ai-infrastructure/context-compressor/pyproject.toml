[project]
name = "context-compressor"
version = "0.1.0"
description = "Select which context items survive a tight window. Selection, never paraphrase."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
