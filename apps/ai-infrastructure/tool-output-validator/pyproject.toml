[project]
name = "tool-output-validator"
version = "0.1.0"
description = "Catch silent tool failures: outputs that look fine and answer nothing."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
