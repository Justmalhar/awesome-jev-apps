[project]
name = "redaction-checker"
version = "0.1.0"
description = "Find PII and confidential detail before a document goes out; one Noul per hazard class, thresholded separately."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
