[project]
name = "invoice-extractor"
version = "0.1.0"
description = "Regex finds candidate spans, Jev selects the right one, Python normalises -- every extracted value is verbatim."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
