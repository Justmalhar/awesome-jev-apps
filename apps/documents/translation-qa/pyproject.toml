[project]
name = "translation-qa"
version = "0.1.0"
description = "Flag translations that changed meaning rather than wording, with per-hazard thresholds."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
