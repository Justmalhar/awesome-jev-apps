[project]
name = "policy-gap-finder"
version = "0.1.0"
description = "Find which controls in a standard your policy document leaves open, vague or unowned — in one Jev request."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
