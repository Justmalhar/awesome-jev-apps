[project]
name = "meeting-actions"
version = "0.1.0"
description = "Commitments, owners and deadlines from a transcript -- owners are selected from the people who spoke, never invented."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
