[project]
name = "rfp-compliance"
version = "0.1.0"
description = "Check a proposal covers every tender requirement: addressed and committed scored separately, in one Jev request."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40", "pandas>=2.0"]
