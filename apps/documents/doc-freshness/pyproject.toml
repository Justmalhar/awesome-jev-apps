[project]
name = "doc-freshness"
version = "0.1.0"
description = "Find the documentation claims the current code no longer backs up."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
