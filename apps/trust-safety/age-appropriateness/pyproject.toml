[project]
name = "age-appropriateness"
version = "0.1.0"
description = "Rate content against age bands the operator defines, with per-concern Nouls and a human tier."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
