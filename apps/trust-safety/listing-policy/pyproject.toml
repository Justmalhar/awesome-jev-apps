[project]
name = "listing-policy"
version = "0.1.0"
description = "Apply a marketplace's own prohibited-items policy to listings: per-clause Nouls plus independent hazard Nouls."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
