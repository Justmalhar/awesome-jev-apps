[project]
name = "scam-detector"
version = "0.1.0"
description = "Screen inbound messages for advance-fee, impersonation, and phishing hazards against an operator's own patterns."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
