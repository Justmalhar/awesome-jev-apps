[project]
name = "ad-brand-safety"
version = "0.1.0"
description = "Judge one ad creative against many placement contexts using the brand's own exclusion rules."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
