[project]
name = "community-moderator"
version = "0.1.0"
description = "Apply an operator's own written community rules to posts: per-rule Nouls, severity Score, human tier."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "streamlit>=1.40"]
